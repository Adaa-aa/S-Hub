diff --git a/app/login.tsx b/app/login.tsx
index bf858c1..692fec1 100644
--- a/app/login.tsx
+++ b/app/login.tsx
@@ -23,7 +23,11 @@ export default function LoginScreen() {
   const T = useThemeColors();
 
   const handleLogin = () => {
-    router.replace('/(tabs)/home' as any);
+    if (role === 'worker') {
+      router.replace('/worker-setup' as any);
+    } else {
+      router.replace('/(tabs)/home' as any);
+    }
   };
 
   return (
diff --git a/app/register.tsx b/app/register.tsx
index 00ec6d8..17eb173 100644
--- a/app/register.tsx
+++ b/app/register.tsx
@@ -27,7 +27,11 @@ export default function RegisterScreen() {
 
   const handleRegister = () => {
     if (!name || !email || !password) return;
-    router.replace('/(tabs)/home');
+    if (role === 'worker') {
+      router.replace('/worker-setup');
+    } else {
+      router.replace('/(tabs)/home');
+    }
   };
 
   return (
